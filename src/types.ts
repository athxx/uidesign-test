export enum TestCase {
  firstPainted = 'first-painted',
  moveAll = 'move-all',
  moveForSelect = 'move-for-select',
  wheelZoom = 'wheel-zoom',
}

export enum Product {
  mastergo = 'mastergo',
  jsDesigner = 'jsDesigner',
  figma = 'figma',
  pixso = 'pixso',
}
