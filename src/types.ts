export enum TestCase {
  firstPainted = 'first-painted',
  moveAll = 'move-all',
  moveForSelect = 'move-for-select',
  wheelZoom = 'wheel-zoom',
}

export enum Product {
  soulma = 'soulma',
  mastergo = 'mastergo',
  xiaopiu = 'xiaopiu',
  figma = 'figma',
  pixso = 'pixso',
  local = 'local',
}
